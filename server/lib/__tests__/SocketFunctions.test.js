const Lib = require('../SocketFunctionNew');

describe("unit tests", () => {
    let socketFunctions;
    beforeAll(()=> {
        socketFunctions = new Lib;
    })

    beforeEach(()=> {
        socketFunctions = new Lib;
    })

    afterEach(()=> {
        socketFunctions = new Lib;
    })

    describe("userlist creation", () => {

    })
})